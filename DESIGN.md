# 自由双选室友平台设计文档

## 1. 文档目的

本文档定义平台第一阶段的产品范围、页面结构、角色权限、核心数据模型和接口边界，作为产品设计、前后端开发与测试验收的共同基线。

平台面向小范围内部使用，用户均为大学生或准大学生，不开放公众注册。学生可以填写室友偏好并发布为“室友卡片”，在发现页按女生/男生分页浏览、查看详情并发起一对一私信。自由选宿舍阶段开启时，学生可以新建宿舍并成为发起人，或通过私信申请卡片加入同性别的未满员宿舍。管理员负责账号导入、身份字段、阶段开关、公开卡片、举报和宿舍状态治理。

## 2. 设计原则

1. **默认保护隐私**：仅展示学生主动发布的信息；敏感字段默认不公开。
2. **最小权限**：学生只能管理自己的室友卡片、会话和宿舍确认；管理员仅获得治理所需权限。
3. **先满足核心流程**：第一阶段不建设复杂的动态权限系统、推荐算法或群聊，私信只服务于双选沟通。
4. **可治理**：公开内容和私信入口必须配套举报、拉黑、封禁和审计能力。
5. **状态明确**：账号、室友卡片、消息、宿舍、加入申请和举报都使用显式状态；学生注销只停用账号，永久删除必须由管理员明确执行并保留审计记录。

## 3. 用户角色

### 3.1 学生（STUDENT）

- 登录、退出并维护自己的密码等安全设置；不能修改管理员预设的姓名、年级和性别。
- 新建、编辑、预览、发布或取消发布自己的室友卡片。
- 浏览已发布的其他学生室友卡片并查看详情。
- 向未拉黑自己的学生发起一对一私信。
- 查看和管理自己参与的会话。
- 创建宿舍、申请加入未满员宿舍，或退出当前宿舍。
- 拉黑或举报其他学生及其公开资料。

### 3.2 管理员（ADMIN）

- 登录管理后台。
- 批量导入学生正式账号，并预设姓名、年级和性别。
- 查询学生账号，修改姓名、年级和性别，并进行启用、停用、封禁或删除操作。
- 查看公开室友卡片，隐藏违规卡片或恢复展示。
- 处理举报并记录处理结论。
- 开启或关闭自由选宿舍阶段，查看并关闭异常宿舍。
- 查看管理操作审计日志。
- 默认**不能读取学生私信正文、冒充学生发信或直接修改学生室友卡片**。

管理员账号不参与学生端室友卡片展示和私信。若未来确有客服介入或合规取证需求，应独立设计审批、授权和审计流程，不在第一阶段预留绕过权限的入口。

## 4. 权限模型

第一阶段采用 RBAC（角色权限）加资源归属校验，不建设可配置的权限表或权限管理后台。用户表保存单一角色 `STUDENT` 或 `ADMIN`，后端在每个受保护接口同时校验角色、账号状态和资源归属。

| 操作 | 学生 | 管理员 | 约束 |
| --- | --- | --- | --- |
| 导入正式学生账号 | 禁止 | 允许 | 必须包含登录标识、姓名和年级 |
| 修改姓名和年级 | 禁止 | 允许 | 管理员修改必须写审计日志 |
| 查看已发布室友卡片 | 允许 | 允许 | 被拉黑关系会限制学生访问 |
| 创建、编辑自己的室友卡片 | 允许 | 不适用 | 仅资源所有者 |
| 发布、取消发布自己的室友卡片 | 允许 | 不适用 | 必填字段完整且账号正常 |
| 编辑他人室友卡片 | 禁止 | 禁止 | 管理员只能隐藏或恢复 |
| 隐藏违规室友卡片 | 禁止 | 允许 | 必须填写原因并写审计日志 |
| 发起和发送私信 | 允许 | 禁止 | 仅会话参与者，且双方均未封禁/拉黑 |
| 查看私信正文 | 仅本人会话 | 禁止 | 服务端按参与者校验 |
| 创建宿舍并加入 | 允许 | 不适用 | 仅阶段开启且当前未加入宿舍 |
| 分配宿舍楼栋和房间号 | 禁止 | 允许 | 学生创建宿舍时不能填写，所有宿舍固定 4 人 |
| 通过私信申请加入宿舍 | 允许 | 不适用 | 仅阶段开启、宿舍未满且申请发送给发起人 |
| 审批宿舍申请 | 仅宿舍发起人 | 禁止 | 审批通过时再次校验容量和唯一归属 |
| 移除宿舍成员 | 仅宿舍发起人 | 禁止 | 不能通过此操作移除自己 |
| 退出自己的宿舍 | 允许 | 不适用 | 发起人退出后自动顺延 |
| 开启/关闭自由选宿舍阶段 | 禁止 | 允许 | 必须填写原因并记录审计日志 |
| 关闭异常宿舍 | 禁止 | 允许 | 已有成员关系保留 |
| 举报、拉黑学生 | 允许 | 不适用 | 不能操作自己 |
| 处理举报 | 禁止 | 允许 | 记录处理人、结论和时间 |
| 停用或封禁学生账号 | 禁止 | 允许 | 必须填写原因并写审计日志 |
| 注销自己的账号 | 允许 | 不适用 | 仅停用账号，不删除任何资源 |
| 永久删除账号或资源 | 禁止 | 允许 | 必须二次确认并写审计日志 |
| 管理管理员账号和角色 | 禁止 | 第一阶段不提供 | 通过受控运维流程完成 |

权限判断必须在后端执行，前端隐藏按钮只用于改善体验，不能作为安全控制。

## 5. 核心功能

### 5.1 账号与登录

- 支持学生和管理员登录、退出以及登录态续期。
- 不提供公开注册入口。学生正式账号只能由管理员通过模板导入，角色固定为 `STUDENT`；管理员账号由受控运维流程创建。
- 导入学生账号时必须预设唯一登录标识、真实姓名和年级。姓名与年级是管理员维护的权威身份字段，学生端只读，所有学生端写接口均拒绝修改。
- 导入时逐行校验必填字段和登录标识唯一性，重复或格式错误的记录不创建账号，并向管理员返回可下载的失败明细。
- 学生首次登录可通过一次性初始密码或统一身份认证完成身份验证，之后只能修改密码等安全设置。
- 账号状态：`PENDING_ACTIVATION`（待激活）、`ACTIVE`（正常）、`SUSPENDED`（停用）、`BANNED`（封禁）、`DEACTIVATED`（用户注销）。非正常账号不能登录、发布卡片、发送消息或组建宿舍。
- 学生注销账号只把状态改为 `DEACTIVATED`，不删除室友卡片、消息、宿舍、举报或上传文件。资源长期保留，只有管理员执行明确的删除操作时才移除。
- 注销后账号不能登录，卡片不再出现在发现页，并退出当前宿舍；历史消息和其他资源仍保存在系统中。

### 5.2 室友卡片

室友卡片由只读身份信息和学生填写的室友偏好组成。姓名、年级直接读取账号身份字段，不由室友卡片保存或修改；室友偏好用于学生自行比较，不自动生成强制匹配结果。

| 字段 | 要求 | 展示位置 |
| --- | --- | --- |
| 姓名 | 管理员导入，学生只读 | 卡片、详情、会话 |
| 年级 | 管理员导入，学生只读 | 卡片、详情 |
| 头像 | 必填，限制格式和大小 | 卡片、详情、会话 |
| 学校/校区 | 必填；统一认证时可锁定 | 卡片、详情 |
| 院系/专业 | 可选 | 卡片、详情 |
| 空调温度偏好 | 必填；夏季和冬季分别填写温度范围 | 卡片、详情 |
| 作息偏好 | 必填；可多选“早起/晚睡/午休”并补充时间 | 卡片、详情 |
| 整洁程度要求 | 必填；使用“宽松/一般/严格”并可补充说明 | 卡片、详情 |
| 个人性格 | 必填；标签多选并可补充说明 | 卡片、详情 |
| 期望室友性格 | 必填；标签多选并可补充说明 | 卡片、详情 |
| 兴趣爱好、喜欢的运动 | 可选，最多 10 个标签并可补充说明 | 卡片、详情 |
| 打游戏要求 | 必填；可选择“经常/偶尔/不接受”并说明时段 | 卡片、详情 |
| 是否介意鼠标键盘声音 | 必填；是/否/视时段而定 | 卡片、详情 |
| 是否介意游戏、刷视频外放 | 必填；是/否/视时段而定 | 卡片、详情 |
| 自认为的一个缺点 | 必填，建议最多 200 字 | 详情，卡片显示摘要 |
| 还想要说的话 | 可选，建议最多 500 字 | 详情，卡片显示摘要 |

温度范围使用数值下限和上限存储，并限制在合理区间；作息、整洁度、声音和游戏要求使用枚举，避免自由文本导致无法比较。学生可以在发布前预览完整卡片。

室友卡片状态：

- `DRAFT`：草稿，仅本人可见。
- `PUBLISHED`：学生发布后立即生效并出现在发现页，无需管理员预审。
- `HIDDEN`：管理员因违规隐藏，学生可查看原因并修改，但不能自行恢复发布；需重新提交管理员处理或由管理员恢复。

发布接口只校验字段完整性、格式、账号状态和基础内容规则，校验通过后直接展示。头像上传时校验 MIME 类型、文件大小和真实文件内容，使用随机文件名保存。资料文本按纯文本展示或使用严格白名单清洗，防止脚本注入。

### 5.3 室友卡片发现页

- 仅返回账号正常且室友卡片状态为 `PUBLISHED` 的学生。卡片发布状态与宿舍成员关系互不耦合。
- 默认按最近更新排序，使用游标分页。
- 支持按学校、年级、院系和兴趣标签筛选，以及按姓名搜索。
- 不展示当前用户自己的室友卡片，也不展示任一方向存在拉黑关系的用户。
- 卡片包含头像、姓名、年级、学校/校区、作息、温度、整洁度、声音容忍度、游戏要求、性格和兴趣等关键摘要。
- 点击卡片进入详情页；详情页提供“发私信”“举报”“拉黑”操作。

第一阶段不做自动匹配或个性化推荐。后续推荐功能只能消费已发布室友卡片，不应绕过拉黑、已组队和卡片可见性规则。

### 5.4 私信与自由选宿舍

- 每两个学生之间最多存在一个会话，重复发起时进入已有会话。私信是自由双选的主要沟通方式，不代表系统自动匹配或默认同意入住。
- 仅会话参与者可以读取该会话及消息。
- 消息第一阶段只支持纯文本，建议单条最多 2000 字。
- 会话列表展示对方摘要、最后一条消息、更新时间和未读数。
- 支持发送、分页加载历史消息、标记已读。
- 任一方拉黑另一方后，双方均不能继续发送新消息，历史消息仍只对原参与者可见；双方之间未处理的宿舍申请自动取消。
- 账号封禁或资料隐藏不删除历史消息；封禁账号不能发送新消息。
- 第一阶段不提供撤回、删除历史消息、群聊、图片或文件消息。宿舍加入申请使用私信中的结构化申请卡片。

#### 宿舍选择规则

- 管理员通过全局开关开启或关闭自由选宿舍阶段。关闭后学生不能创建、申请、审批、移除或退出宿舍。
- 学生可以新建宿舍并立即加入，第一个加入者成为宿舍发起人。
- 所有宿舍固定为 4 人间。学生创建时只填写宿舍名称，楼栋和房间号由管理员统一分配。
- 未加入宿舍的学生可以浏览所有同性别宿舍；未满员宿舍可进入发起人私信发送申请，已满宿舍仅展示状态。
- 发起人可以在私信申请卡片或宿舍页批准、拒绝申请；批准时再次校验容量以及申请人未加入其他宿舍。
- 发起人可以移除当前宿舍的普通成员。只要阶段开启，所有成员在宿舍满员后仍可主动退出。
- 发起人退出后，最早加入的成员自动成为新发起人；宿舍无人后直接从数据库删除。
- 每名学生同一时间最多加入一个宿舍，一个宿舍不能超过设定容量。

为防止骚扰，应对新会话和消息发送设置频率限制，并允许学生随时举报或拉黑。

### 5.5 举报与治理

学生可举报“用户/室友卡片”或“会话中的消息”，举报原因使用枚举并可补充说明。举报快照应保存被举报内容在举报时的必要副本，避免内容变化后无法核查。

举报状态：`PENDING`（待处理）、`RESOLVED`（已处理）、`REJECTED`（不成立）。管理员可以采取无操作、隐藏资料或封禁账号等措施，所有操作必须写入审计日志。

为了遵守管理员默认不可查看私信的规则，消息举报只向管理员展示学生主动提交的被举报消息及必要上下文，不开放整个会话历史。

## 6. 页面与导航

### 6.1 学生端

| 页面 | 主要内容 |
| --- | --- |
| 登录页 | 身份认证、错误提示 |
| 室友发现页 | 女生/男生分页、筛选栏、搜索和室友卡片列表 |
| 室友卡片详情页 | 完整公开偏好；自己的卡片显示标识和编辑入口，其他卡片提供私信、举报和拉黑 |
| 我的室友卡片页 | 编辑问卷、头像上传、预览、发布状态和违规原因 |
| 消息页 | 左侧会话列表、右侧消息历史与输入框；移动端拆为两级页面 |
| 自由选宿舍页 | 阶段状态、创建宿舍、浏览同性别的未满/已满宿舍、当前成员、待审申请和退出 |
| 账号设置页 | 只读姓名与年级、密码设置、退出登录、账号注销、拉黑列表 |

### 6.2 管理后台

| 页面 | 主要内容 |
| --- | --- |
| 管理员登录页 | 独立入口和管理员认证 |
| 概览页 | 待处理举报数、活跃账号数、已发布资料数 |
| 用户管理页 | 批量导入账号、查询、修改姓名/年级、停用、封禁、恢复和删除 |
| 室友卡片管理页 | 查询公开卡片、隐藏和恢复 |
| 宿舍管理页 | 查询宿舍成员与状态、关闭异常宿舍 |
| 举报处理页 | 举报详情、必要快照、处理措施和结论 |
| 审计日志页 | 操作人、动作、对象、原因、时间和来源 IP |

## 7. 关键业务流程

### 7.1 发布室友卡片

1. 学生登录后进入“我的室友卡片”。
2. 填写室友偏好问卷并上传头像，保存为草稿。
3. 点击发布，后端校验必填字段、账号状态和内容规则。
4. 校验成功后状态立即改为 `PUBLISHED`，无需管理员审核，卡片进入室友发现页。
5. 若管理员隐藏卡片，学生看到原因，修改后等待管理员恢复。

### 7.2 发起私信

1. 学生从室友卡片详情点击“发私信”。
2. 后端检查双方账号、资料可见性和拉黑关系。
3. 查询双方已有会话；没有则创建，有则复用。
4. 进入会话并发送文本消息。
5. 后端写入消息、更新会话最后活动时间并增加对方未读数。

### 7.3 自由选择宿舍

1. 管理员开启自由选宿舍阶段。
2. 未加入宿舍的学生可以新建宿舍并立即成为发起人，或浏览同性别的未满和已满宿舍。
3. 申请加入时进入宿舍发起人的私信，在会话中填写并发送加入申请卡片。
4. 发起人从私信卡片或宿舍页批准或拒绝申请。
5. 批准时后端再次校验阶段开启、宿舍未满以及申请人未加入其他宿舍，然后建立成员关系。
6. 发起人可以移除普通成员；成员可以退出。发起人退出后，最早加入的成员自动接任。

## 8. 数据模型

以下为逻辑模型，字段类型和索引按最终数据库确定。

### `users`

- `id`
- `role`: `STUDENT | ADMIN`
- `login_identifier`（唯一）
- `name`, `grade`, `gender`（管理员导入和维护）
- `email` 或统一认证标识（按实际登录方式选择）
- `password_hash`（仅本地认证时存在）
- `status`: `PENDING_ACTIVATION | ACTIVE | SUSPENDED | BANNED | DEACTIVATED`
- `imported_by`, `deactivated_at`
- `created_at`, `updated_at`, `last_login_at`

### `roommate_cards`

- `id`, `user_id`（唯一）
- `avatar_url`
- `school`, `campus`, `department`
- `summer_temp_min`, `summer_temp_max`, `winter_temp_min`, `winter_temp_max`
- `sleep_preferences`, `sleep_schedule_note`
- `cleanliness_level`, `cleanliness_note`
- `personality_tags`, `personality_note`
- `roommate_personality_tags`, `roommate_personality_note`
- `hobbies`, `sports`, `hobbies_note`
- `gaming_frequency`, `gaming_time_note`
- `keyboard_noise_tolerance`, `media_noise_tolerance`
- `self_acknowledged_shortcoming`, `additional_note`
- `status`: `DRAFT | PUBLISHED | HIDDEN`
- `hidden_reason`, `published_at`
- `created_at`, `updated_at`

`name` 和 `grade` 不在本表重复存储。卡片展示时通过 `user_id` 读取 `users.name` 和 `users.grade`，学生更新室友卡片的请求体中不得接受这两个字段。

### `roommate_card_tags`

- `card_id`, `tag_id`
- 对 `(card_id, tag_id)` 建唯一约束

### `tags`

- `id`, `name`, `status`

### `conversations`

- `id`
- `student_a_id`, `student_b_id`，按较小/较大用户 ID 固定顺序存储
- `last_message_at`, `created_at`
- 对 `(student_a_id, student_b_id)` 建唯一约束

### `messages`

- `id`, `conversation_id`
- `sender_id`
- `body`
- `message_type`: `TEXT | DORMITORY_APPLICATION`
- `application_id`（申请卡片消息时存在）
- `created_at`
- 按 `(conversation_id, created_at, id)` 建分页索引

### `conversation_reads`

- `conversation_id`, `user_id`
- `last_read_message_id`, `updated_at`
- 对 `(conversation_id, user_id)` 建唯一约束

### `dormitories`

- `id`, `dormitory_code`, `name`, `building`, `room_number`
- `capacity`（固定为 4）、`initiator_id`、`gender`
- `building`, `room_number`（仅管理员分配）
- `status`: `OPEN | FULL | CLOSED`
- `created_at`, `updated_at`

### `dormitory_members`

- `dormitory_id`, `user_id`
- `role`: `INITIATOR | MEMBER`
- `joined_at`
- `user_id` 唯一，保证每名学生最多加入一个宿舍

### `dormitory_applications`

- `id`, `dormitory_id`, `applicant_id`, `conversation_id`, `message_id`
- `note`
- `status`: `PENDING | APPROVED | REJECTED | CANCELLED`
- `reviewed_by`, `reviewed_at`, `created_at`, `updated_at`

### `system_settings`

- `key`: `dormitory_selection_open`
- `value`, `updated_by`, `updated_at`

### `blocks`

- `blocker_id`, `blocked_id`, `created_at`
- 对 `(blocker_id, blocked_id)` 建唯一约束

### `reports`

- `id`, `reporter_id`
- `target_type`: `ROOMMATE_CARD | MESSAGE`
- `target_id`, `reason`, `description`
- `snapshot`
- `status`: `PENDING | RESOLVED | REJECTED`
- `handled_by`, `resolution`, `created_at`, `handled_at`

### `audit_logs`

- `id`, `admin_id`
- `action`, `target_type`, `target_id`
- `reason`, `metadata`
- `ip_address`, `created_at`

## 9. 接口边界

接口统一以 `/api` 为前缀，使用 HttpOnly Cookie 中的短时 Access Token 认证；写操作同时校验同源和 CSRF Token。Refresh Token 只用于 `/api/auth` 下的轮换与注销接口。

### 认证接口

- `POST /api/auth/login`
- `POST /api/auth/refresh`
- `POST /api/auth/logout`

### 学生端接口

- `GET /api/me`
- `POST /api/me/deactivate`
- `GET /api/me/roommate-card`
- `PUT /api/me/roommate-card`
- `POST /api/me/roommate-card/avatar`
- `POST /api/me/roommate-card/publish`
- `POST /api/me/roommate-card/unpublish`
- `GET /api/roommate-cards`
- `GET /api/roommate-cards/{cardId}`
- `POST /api/roommate-cards/{cardId}/conversations`
- `POST /api/users/{userId}/conversations`
- `GET /api/dormitory-selection`
- `GET /api/dormitories`
- `POST /api/dormitories`
- `GET /api/dormitories/{dormitoryId}`
- `GET /api/me/dormitory`
- `POST /api/me/dormitory/leave`
- `POST /api/conversations/{conversationId}/dormitory-applications`
- `POST /api/dormitory-applications/{applicationId}/approve`
- `POST /api/dormitory-applications/{applicationId}/reject`
- `DELETE /api/dormitories/{dormitoryId}/members/{userId}`
- `GET /api/conversations`
- `GET /api/conversations/{conversationId}/messages`
- `POST /api/conversations/{conversationId}/messages`
- `POST /api/conversations/{conversationId}/read`
- `POST /api/users/{userId}/blocks`
- `DELETE /api/users/{userId}/blocks`
- `POST /api/reports`

### 管理端接口

- `GET /api/admin/users`
- `POST /api/admin/users/import`
- `PATCH /api/admin/users/{userId}/identity`
- `PATCH /api/admin/users/{userId}/status`
- `DELETE /api/admin/users/{userId}`
- `GET /api/admin/roommate-cards`
- `POST /api/admin/roommate-cards/{cardId}/hide`
- `POST /api/admin/roommate-cards/{cardId}/restore`
- `GET /api/admin/settings/dormitory-selection`
- `PATCH /api/admin/settings/dormitory-selection`
- `GET /api/admin/dormitories`
- `POST /api/admin/dormitories/{dormitoryId}/close`
- `GET /api/admin/reports`
- `GET /api/admin/reports/{reportId}`
- `POST /api/admin/reports/{reportId}/resolve`
- `GET /api/admin/audit-logs`

列表接口采用游标分页并限制最大页大小。错误响应使用统一错误码，例如 `UNAUTHORIZED`、`FORBIDDEN`、`ROOMMATE_CARD_NOT_PUBLISHED`、`USER_BLOCKED` 和 `RATE_LIMITED`。

## 10. 安全与隐私

- 所有对象访问均在服务端进行角色与资源归属校验，防止通过修改 ID 越权访问。
- 密码使用 Argon2id 或 bcrypt 哈希；登录、发消息、上传和举报接口均限流。
- 全站使用 HTTPS；Cookie 设置 `HttpOnly`、`Secure` 和合适的 `SameSite`。
- 数据库查询使用参数化语句，资料和消息输出进行 HTML 转义。
- 上传文件限制类型、大小和数量，拒绝可执行内容；对象存储默认私有，通过受控地址访问。
- 管理员关键操作要求重新确认，全部进入不可由普通管理员修改的审计日志。
- 日志中不得记录密码、令牌、完整私信正文或不必要的个人信息。
- 系统不设置账号资源的自动过期或自动清理任务。学生注销后所有资源长期保留，但不再对外展示其卡片。
- 管理员永久删除账号时，系统删除该账号、室友卡片、上传文件和其拥有的业务资源；对共享会话等存在关联的数据按事务级联处理，避免残留越权入口。
- 管理员删除属于不可逆高风险操作，必须二次确认、填写原因并写入审计日志。审计日志只保留被删除资源 ID、操作人、原因和时间，不保留已删除正文。
- 用户均为大学生或准大学生，系统不对公众开放。准大学生可能未满 18 岁，上线前仍需确认适用的告知、同意和个人信息保护要求。

## 11. 非功能要求

- **兼容性**：优先适配手机，同时支持主流桌面浏览器。
- **性能**：常规列表和详情接口目标 P95 小于 500 ms（不含文件上传和外部认证）。
- **可用性**：所有列表提供加载、空数据、错误和重试状态；发送消息显示明确成功或失败状态。
- **可访问性**：键盘可操作、表单有标签、颜色对比度满足 WCAG 2.1 AA 的核心要求。
- **可观测性**：记录请求 ID、接口错误率、消息发送失败率、举报积压和管理员治理操作。
- **备份**：数据库定期备份并验证恢复流程；头像等文件按存储策略备份或可重建。

## 12. 第一阶段范围

### 必须交付

- 管理员导入正式账号、维护只读姓名/年级，以及学生与管理员登录和角色隔离。
- 室友卡片问卷编辑、预览、发布和取消发布。
- 室友卡片校验通过后直接展示，不设置管理员预审队列。
- 室友卡片发现、筛选、搜索和详情。
- 一对一纯文本私信、会话列表与未读状态。
- 新建宿舍、浏览未满宿舍、私信申请卡片、发起人审批和成员退出。
- 自由选宿舍阶段开关、成员移除、拉黑、举报、管理员治理和审计。
- 移动端与桌面端基础适配。

### 暂不接入

- 好友关系、关注、点赞、评论和动态广场。
- 群聊、图片、语音、视频和文件消息。
- 自动匹配、个性化推荐、在线状态和已读回执展示。
- 复杂组织层级、多管理员角色和可配置权限后台。
- 支付、积分、活动报名和第三方社交账号导流。

后续功能接入时，必须为学生端操作和管理员治理分别定义权限，并复用账号状态、资源归属、拉黑关系和审计规则；不要通过前端开关代替服务端授权。

## 13. 验收标准

1. 系统没有公开注册入口，只有管理员导入且状态正常的正式账号可以登录。
2. 姓名和年级由管理员预设；学生通过页面或直接调用接口均不能修改，管理员修改后产生审计记录。
3. 学生只能修改自己的室友偏好卡片，无法通过接口访问他人的草稿或隐藏卡片。
4. 室友卡片发布校验成功后立即出现在发现页，不进入管理员预审队列。
5. 未发布、被隐藏或账号异常的室友卡片不会出现在发现页；宿舍关系不改变卡片发布状态。
6. 学生只能查看自己参与的会话，修改会话或消息 ID 不能越权读取。
7. 拉黑后无法新建会话或继续发送消息，双方室友卡片互相不可见。
8. 管理员可以治理公开室友卡片、举报和异常宿舍，但无法通过管理接口读取完整私信或代替发起人审批普通申请。
9. 阶段关闭后，学生不能创建、申请、审批、移除或退出宿舍；现有数据保持不变。
10. 每名学生最多加入一个宿舍，宿舍人数不能超过容量，首位成员为发起人。
11. 学生注销后无法登录且卡片不再展示，但原有卡片、消息、举报和上传文件仍保留；只有管理员删除后资源才消失。
12. 所有身份字段修改、阶段开关、封禁、隐藏、恢复、永久删除、宿舍关闭和举报处理操作都有可检索的审计记录。
13. 手机和桌面端均可完整完成卡片发布、浏览卡片、私信申请和宿舍管理流程。

## 14. 待确认事项

以下细节仍需在进入开发前确认：

- 管理员导入模板采用学号、邮箱还是其他字段作为唯一登录标识，以及首次登录使用一次性密码还是统一身份认证。
- 学校、院系和专业是否来自固定字典；姓名和年级已确定只能由管理员修改。
- 是否允许跨学校浏览与私信。
- 内容审核采用人工处理、关键词规则还是外部审核服务。
- 管理员永久删除整个账号时，共享会话与举报快照的具体级联删除范围。
- 准大学生中如包含未满 18 岁用户，是否需要额外的监护人告知或同意流程。
